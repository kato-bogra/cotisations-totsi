services:
  - type: web
    name: cotisations-totsi
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python database.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
